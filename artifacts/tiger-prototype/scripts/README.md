# Prototype audit scripts

Run from `artifacts/tiger-prototype`:

```bash
node scripts/audit-data.mjs          # stage/objection/prompt counts
npx tsx scripts/test-vapi-session.mjs  # PATCH assistant for sample scenarios (needs network)
```
